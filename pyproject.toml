[project]
name = "hermes-desktop-theme-pack"
version = "1.0.0"
description = "Curated color themes for Hermes Agent with a Theme Switcher desktop plugin"
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=7,<9", "pyyaml>=6,<7"]
