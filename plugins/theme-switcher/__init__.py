"""theme-switcher — Hermes plugin entry point (stub).

This plugin exists so the dashboard backend (dashboard/plugin_api.py) is
discoverable and enableable via ``hermes plugins enable theme-switcher``.
It registers no agent tools; its payload is the skin browser API mounted
by the desktop backend under /api/plugins/theme-switcher/.
"""

from __future__ import annotations
